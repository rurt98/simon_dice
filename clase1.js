var nombre = "Uriel", apellido = "Rodriguez";

var nombreEnMayusculas = nombre.toUpperCase();

var apellidoEnMayusculas = apellido.toLowerCase();

var primerLetraDelNombre = nombre.charAt(0); // para la primera letra del string

var cantidadDeLasLetras = nombre.length;

var nombreCompleto = `${nombre} ${apellido.toUpperCase()}`;

var str = nombre.substr(4);

